# ResizeImagesToPixelCount 节点说明

## 功能介绍
ResizeImagesToPixelCount 节点用于将输入的图像调整到指定的像素数量，同时保持图像的宽高比。这对于需要统一图像尺寸但又不想破坏图像比例的场景非常有用。

## 输入参数
1. **images**: 输入的图像批次 (IMAGE 类型)
2. **pixel_count**: 目标像素数量 (INT 类型，默认值: 1048576，范围: 64-10000000)
3. **steps**: 计算步数 (INT 类型，默认值: 64，范围: 1-1000)

## 输出
- **IMAGE**: 调整大小后的图像批次

## 使用方法
1. 将需要调整大小的图像连接到 images 输入端口
2. 设置目标像素数量 (例如: 1024x1024 = 1048576 像素)
3. 调整 steps 参数以控制计算精度 (数值越大越精确但计算时间越长)
4. 连接输出到后续节点

## 注意事项
- 节点会尽量保持原始图像的宽高比
- 调整后的图像尺寸可能与理论值略有差异，这是为了保持最佳的宽高比
- 对于大批次图像处理，建议适当降低 steps 参数以提高性能